{
  "name": "fee-testing-script",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate-type": "abi-types-generator './src/contract/abi/vault.json' --output='./src/contract/abi/types'  --provider=ethers",
    "start:dev": "sudo npm run generate-type && npx nodemon",
    "build": "rimraf ./build && tsc",
    "start:prod": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.1",
    "nodemon": "^2.0.21",
    "rimraf": "^4.4.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "ethers": "^6.1.0"
  }
}
